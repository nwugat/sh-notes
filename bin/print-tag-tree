#!/usr/bin/env sh

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
# source "$SCRIPT_DIR/../lib/common.sh"

$SCRIPT_DIR/print-tags -a | sort | uniq | $SCRIPT_DIR/../scripts/to-tree.py
